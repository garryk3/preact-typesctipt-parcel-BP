import { h } from "preact";
import { memo } from 'preact/compat';

const Sidebar = (props: {}) => {
    return (
        <div>
            sidebar
        </div>
    )
}

export default memo(Sidebar);